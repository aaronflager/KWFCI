@model KWFCI.Models.ViewModels.AdminVM


@{ 
    //Layout = null;
}
<div>
    <div class="col-xs-12">
        <table class="table table-bordered editTable">
            <thead>
                <tr>
                    <th>Edit</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (StaffProfile sp in Model.Staff)
                {
                    <tr class="staff-parentElem">
                        <td>
                            <button class="btn btn-primary btn-md" data-toggle="modal" data-target="#adminEditStaff"
                               data-id="@sp.StaffProfileID">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </button>
                        </td>
                        <td class="staff-firstName">
                            @sp.FirstName
                        </td>

                        <td class="staff-lastName">
                            @sp.LastName
                        </td>

                        <td class="staff-email">
                            @sp.Email
                        </td>

                        <td class="staff-role">
                            @sp.Role
                        </td>

                        <td>
                            <form asp-action="StaffDelete" asp-route-id="@sp.StaffProfileID" method="post">
                                <button type="submit"
                                        class="btn btn-sm btn-danger center-block">
                                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        @Html.Partial("Modals/_AdminEditStaffModal", Model.NewStaff)
    </div>
</div>

<style>
    table, tr, th {
        border: 1px solid black;
    }
</style>
